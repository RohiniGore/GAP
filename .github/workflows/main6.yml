name: updated branching strategy workflow
 
on:
  push:
    branches:
      - main
      - develop
    tags-ignore:
      - 'v1.*' 
jobs: 
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print build
        run: echo "build"
 
  test:
    runs-on: windows-latest
    steps:
      - name: Print test - chrome
        run: echo "test chrome"
      - name: Print test - edge
        run: echo "test edge"
 
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print deploy
        run: echo "deploy
